const body = document.querySelector('body');
body.className =
    'p-4 bg-gradient-to-r from-violet-400 via-pink-400 to-red-400 flex-col items-start ';
const section = document.querySelector('.container');
section.className = 'flex flex-wrap w-4/5 m-auto';
const addButton = document.querySelector('.btnAdd');
addButton.className =
    'w-12 h-12 bg-slate-500 rounded-full pt-2 text-white text-center text-2xl font-bold m-auto';
