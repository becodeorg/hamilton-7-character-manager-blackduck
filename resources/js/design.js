const body = document.querySelector('body');
body.className =
    'bg-gradient-to-r from-violet-400 via-pink-400 to-red-400 flex-col items-start';
const section = document.querySelector('.container');
section.className = 'flex flex-wrap';
